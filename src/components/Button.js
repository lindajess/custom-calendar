import React from 'react';
import Button from '@material-ui/core/Button';

const CButton = ({ onClick }) => <Button variant="contained" color="primary" onClick={onClick}> Create Reminder </Button>

export default CButton
